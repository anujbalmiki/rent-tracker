# Copy to secrets.toml and fill in real values (never commit secrets.toml).

[mongo]
uri = "mongodb+srv://USER:PASSWORD@cluster.example.mongodb.net/?retryWrites=true&w=majority"

[admin]
username = "admin"
password = "change-me-to-a-strong-password"
